let str=(" javascript developer ")

//backticks--> extended functionality
console.log(`1+2=${1+2}`);

//to make run on the terminal we use node nameOfTheFile,
//length is a property(push,pop)
console.log(str.length)

//to acccess an individual character os a string we use ([])
console.log(str[1])